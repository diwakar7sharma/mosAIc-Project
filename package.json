{
  "name": "meeting-actioner",
  "version": "1.0.0",
  "description": "AI-powered meeting transcript analyzer with task extraction and voice summaries",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd server && npm run dev",
    "client:dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["meeting", "ai", "transcript", "tasks", "voice", "openai", "elevenlabs"],
  "author": "Meeting Actioner Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
